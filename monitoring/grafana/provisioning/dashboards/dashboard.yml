# Grafana仪表板配置
apiVersion: 1

providers:
  - name: 'RQA2025 Dashboards'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards