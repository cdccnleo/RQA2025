/**
 * Luxon 3.4.4 - Local Copy
 * Simplified version for offline usage
 *
 * Note: This is a placeholder. For production, download the full version from:
 * https://cdn.jsdelivr.net/npm/luxon@3.4.4/build/global/luxon.min.js
 */

// Check if luxon is already defined (avoid double loading)
if (typeof luxon === 'undefined') {
    // Minimal Luxon implementation for basic functionality
    window.luxon = {
        DateTime: class DateTime {
            constructor() {
                this.date = new Date();
            }

            static now() {
                return new DateTime();
            }

            static local() {
                return new DateTime();
            }

            static fromISO(iso) {
                const dt = new DateTime();
                dt.date = new Date(iso);
                return dt;
            }

            toFormat(format) {
                return this.date.toLocaleString();
            }

            toISO() {
                return this.date.toISOString();
            }

            plus(duration) {
                return this;
            }

            minus(duration) {
                return this;
            }

            startOf(unit) {
                return this;
            }

            endOf(unit) {
                return this;
            }
        },

        Duration: class Duration {
            constructor(obj) {
                this.values = obj || {};
            }

            static fromObject(obj) {
                return new Duration(obj);
            }

            toMillis() {
                return 0;
            }
        },

        Interval: class Interval {
            constructor(start, end) {
                this.start = start;
                this.end = end;
            }

            static fromDateTimes(start, end) {
                return new Interval(start, end);
            }

            length(unit) {
                return 0;
            }
        }
    };

    console.log('Luxon placeholder initialized');
}
