{
  "smtp_server": "${EMAIL_SMTP_SERVER}",
  "smtp_port": "${EMAIL_SMTP_PORT}",
  "username": "${EMAIL_SENDER_USER}",
  "password": "${EMAIL_SENDER_PASS}",
  "from_email": "${EMAIL_SENDER_ADDRESS}",
  "to_emails": ["${EMAIL_RECEIVER_LIST}"]
}
