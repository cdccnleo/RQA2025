# 文档重组计划

## 📋 重组目标

按照项目代码结构（src目录）重新组织docs目录，实现文档与代码的对应关系，便于快速索引和查找。

## 🏗️ 新的文档结构

### 1. 核心模块文档 (docs/core/)
- 对应 `src/core/` 模块
- 包含接口设计、事件总线、容器等核心功能文档

### 2. 交易模块文档 (docs/trading/)
- 对应 `src/trading/` 模块
- 包含交易引擎、订单管理、实时执行、风控等文档

### 3. 数据模块文档 (docs/data/)
- 对应 `src/data/` 模块
- 包含数据管理、验证器、加载器、数据流等文档

### 4. 模型模块文档 (docs/models/)
- 对应 `src/models/` 模块
- 包含模型评估、基础模型、模型管理等文档

### 5. 特征工程文档 (docs/features/)
- 对应 `src/features/` 模块
- 包含特征工程、信号生成、情感分析等文档

### 6. 适配器文档 (docs/adapters/)
- 对应 `src/adapters/` 模块
- 包含MiniQMT等第三方系统适配器文档

### 7. 基础设施文档 (docs/infrastructure/)
- 对应 `src/infrastructure/` 模块
- 包含系统基础设施、监控、日志等文档

### 8. 服务模块文档 (docs/services/)
- 对应 `src/services/` 模块
- 包含各种服务层文档

### 9. 引擎模块文档 (docs/engine/)
- 对应 `src/engine/` 模块
- 包含各种引擎组件文档

### 10. 回测模块文档 (docs/backtest/)
- 对应 `src/backtest/` 模块
- 包含回测引擎、分析器等文档

### 11. 集成模块文档 (docs/ensemble/)
- 对应 `src/ensemble/` 模块
- 包含模型集成、组合策略等文档

### 12. 调优模块文档 (docs/tuning/)
- 对应 `src/tuning/` 模块
- 包含模型调优、参数优化等文档

### 13. 加速模块文档 (docs/acceleration/)
- 对应 `src/acceleration/` 模块
- 包含性能优化、并行计算等文档

### 14. 工具模块文档 (docs/utils/)
- 对应 `src/utils/` 模块
- 包含通用工具、辅助函数等文档

## 📁 保留的通用文档目录

### 架构文档 (docs/architecture/)
- 系统整体架构设计
- 模块间关系图
- 架构演进历史

### 部署文档 (docs/deployment/)
- 部署指南和脚本
- 环境配置
- 容器化部署

### 测试文档 (docs/testing/)
- 测试策略和规范
- 测试用例文档
- 测试工具使用

### 配置文档 (docs/configuration/)
- 系统配置指南
- 环境变量说明
- 配置文件模板

### 报告文档 (docs/reports/)
- 项目进度报告
- 性能分析报告
- 问题修复报告

### API文档 (docs/api/)
- API接口文档
- 接口使用示例
- 错误码说明

## 🔄 迁移计划

### 第一阶段：创建新目录结构
1. 创建按代码结构组织的文档目录
2. 移动现有文档到对应目录
3. 更新文档索引

### 第二阶段：补充缺失文档
1. 为每个模块创建README.md
2. 补充模块设计文档
3. 添加使用示例

### 第三阶段：优化和验证
1. 验证文档完整性
2. 更新文档索引
3. 建立文档维护流程

## 📊 文档索引更新

更新 `docs/DOCUMENT_INDEX.md` 以反映新的组织结构，确保：
- 按模块分类的文档索引
- 快速定位功能
- 清晰的导航结构

## ✅ 完成标准

- [ ] 所有文档按代码结构重新组织
- [ ] 每个模块都有对应的文档目录
- [ ] 文档索引更新完成
- [ ] 建立文档维护规范 