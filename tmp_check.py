from src.data.compliance.compliance_checker import ComplianceChecker\nfrom src.data.compliance.data_policy_manager import DataPolicyManager\nimport pandas as pd\nmanager = DataPolicyManager()\npolicy = {'id': 'sample_policy', 'name': 'Sample', 'required_fields': ['user_id', 'status'], 'field_types': {'user_id': 'integer', 'status': 'string'}, 'max_field_lengths': {}, 'business_rules': {'enum_values': {'status': ['active', 'inactive']}}}\nmanager.register_policy(policy)\nchecker = ComplianceChecker(manager)\ndf = pd.DataFrame({'user_id': [None, 123], 'status': [None, 'active']})\nprint(checker.check(df, 'sample_policy'))
