## 问题诊断结果

### 🔍 根本原因

特征任务 **task_1771162936** 创建成功并保存到持久化存储，但**没有成功提交到调度器队列**！

### 📋 证据

1. **任务已保存**：
   ```
   2026-02-15 21:42:16,058 - src.gateway.web.feature_task_persistence - INFO - 特征提取任务已保存: task_1771162936
   ```

2. **没有看到 submit_task 成功日志**：
   - 没有看到 `任务已提交到调度器: task_1771162936` 的日志
   - 说明 `scheduler.submit_task()` 调用失败或抛出异常

3. **任务状态**：
   - 任务状态为 `pending`（等待中）
   - 但调度器队列中没有该任务
   - 工作节点无法获取任务执行

### 🐛 代码问题

在 `submit_feature_task` 函数中（第279-299行）：

```python
# 提交任务到调度器
try:
    from src.features.distributed.task_scheduler import (
        get_task_scheduler, TaskPriority
    )
    scheduler = get_task_scheduler()
    # 启动调度器（如果未启动）
    if not scheduler._running:
        scheduler.start()
    
    # 提交任务到调度器
    scheduler_task_id = scheduler.submit_task(...)
    logger.info(f"任务已提交到调度器: {task_id} (调度器ID: {scheduler_task_id})")
except Exception as e:
    logger.warning(f"提交任务到调度器失败: {e}")
    # 即使调度器失败，任务记录已创建，可以手动执行
```

**问题**：
1. 异常被捕获后只记录警告日志，没有重新抛出
2. 任务创建成功但调度器提交失败，导致任务状态不一致
3. 用户看到任务已创建，但任务永远不会被执行

### 🛠️ 修复方案

#### 方案1: 修复任务提交流程（推荐）

修改 `submit_feature_task` 函数：

1. **确保调度器已启动并有工作节点**：
   - 检查调度器是否运行
   - 检查是否有工作节点
   - 如果没有，调用 `start_with_workers()`

2. **改进错误处理**：
   - 如果提交失败，更新任务状态为 `failed`
   - 记录详细错误信息
   - 提供失败原因给用户

3. **添加任务状态同步**：
   - 提交成功后，更新任务状态为 `submitted`
   - 确保持久化存储和调度器状态一致

#### 方案2: 修复调度器初始化顺序

在 `start_scheduler()` 中确保：
1. 先启动调度器
2. 创建工作节点
3. 再重新提交 pending 任务

#### 方案3: 添加任务自动重试机制

在 `resubmit_pending_tasks()` 中：
1. 定期检查 pending 状态的任务
2. 自动提交到调度器
3. 避免任务一直处于 pending 状态

### 📁 需要修改的文件

1. `src/gateway/web/feature_engineering_service.py`
   - 修复 `submit_feature_task` 函数
   - 改进错误处理和状态同步

2. `src/gateway/web/feature_task_persistence.py`
   - 添加任务状态更新函数

### ✅ 预期结果

- 任务创建后立即提交到调度器
- 调度器队列中有等待的任务
- 工作节点能够获取并执行任务
- 任务状态正确反映实际执行情况