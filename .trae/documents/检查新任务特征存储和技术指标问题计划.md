# 检查新任务特征存储和技术指标问题计划

## 任务目标
检查新创建的特征提取任务 `feature_task_single_002837_1771757472` 的特征存储情况，并分析为什么技术指标（如 SMA, EMA, RSI, MACD）不在特征存储列表中。

## 检查步骤

### 1. 查询任务状态和配置
- 检查任务 `feature_task_single_002837_1771757472` 在 `feature_engineering_tasks` 表中的状态
- 查看任务配置中的 `indicators` 字段，确认配置了哪些技术指标
- 确认任务状态为 "completed" 和 `feature_count`

### 2. 查询特征存储表数据
- 检查 `feature_store` 表中该任务的特征数据
- 验证特征数量与任务记录是否一致
- 检查特征名称列表，确认是否包含技术指标

### 3. 分析技术指标缺失原因
- 对比任务配置中的 `indicators` 与实际生成的特征列表
- 检查 `worker_executor.py` 中特征生成的逻辑
- 分析为什么配置的技术指标（SMA, EMA, RSI, MACD）没有生成对应的特征

### 4. 检查特征生成逻辑
- 检查特征引擎是否正确计算技术指标
- 检查 `processed_df.columns` 是否包含技术指标列
- 分析特征列表 `features_list` 的生成过程

### 5. 验证修复
- 确认问题原因
- 提出修复方案
- 验证修复后的结果

## 预期结果
- 任务状态为 "completed"
- `feature_store` 表中有该任务的特征记录
- 特征列表应包含配置的技术指标（如 SMA_5, EMA_10, RSI_14 等）
- 而不仅仅是基础价格数据（open, high, low, close, volume）
