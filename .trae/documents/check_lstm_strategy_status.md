# 检查 LSTM策略-182646 状态设置和生命周期仪表盘取值逻辑计划

## 目标
验证已部署的LSTM策略-182646状态是否正确设置，并检查策略生命周期管理页面中运行中仪表盘的取值逻辑。

## 检查范围

### 1. 策略状态检查
- [ ] 检查策略构思在数据库中的状态
- [ ] 检查生命周期记录的状态
- [ ] 检查策略执行状态

### 2. 生命周期仪表盘检查
- [ ] 检查运行中策略数量统计逻辑
- [ ] 检查仪表盘数据来源
- [ ] 检查状态筛选和计算逻辑

### 3. 数据一致性检查
- [ ] 检查数据库和文件系统数据一致性
- [ ] 检查策略列表显示状态
- [ ] 检查生命周期流程显示

## 具体检查项

### 数据库检查
```sql
-- 检查点：
-- 1. strategy_conceptions 表中LSTM策略的状态
-- 2. strategy_lifecycle 表中对应记录的状态
-- 3. 策略当前生命周期阶段
```

### 文件系统检查
```
/app/data/lifecycles/
└── model_strategy_*.json

-- 检查点：
-- 1. 生命周期文件是否存在
-- 2. current_status 字段值
-- 3. current_stage 字段值
```

### 前端仪表盘检查
```javascript
// 检查点：
// 1. 运行中策略数量如何计算
// 2. 状态筛选逻辑
// 3. 数据刷新机制
```

## 验证步骤

### 步骤 1: 数据库验证
1. 查询 strategy_conceptions 表
2. 查询 strategy_lifecycle 表
3. 验证策略状态字段

### 步骤 2: 文件系统验证
1. 检查生命周期文件
2. 验证状态字段值
3. 检查事件记录

### 步骤 3: 前端逻辑验证
1. 检查策略列表加载逻辑
2. 检查仪表盘统计逻辑
3. 检查状态筛选逻辑

### 步骤 4: 问题修复
1. 修复状态设置问题
2. 修复仪表盘取值逻辑
3. 验证修复效果

## 预期结果

### 正常状态
1. **策略构思**: status = 'active' 或 'running'
2. **生命周期**: current_status = 'live_trading' 或 'running'
3. **仪表盘**: 正确统计运行中策略数量

### 状态流转
```
development → backtest → paper_trading → live_trading → retired
   (开发)      (回测)       (模拟交易)       (实盘交易)      (退市)
```

## 问题排查

### 常见问题
1. **状态未更新**: 部署后状态未正确转换
2. **仪表盘不显示**: 状态筛选条件不匹配
3. **数据不一致**: 数据库和文件系统状态不一致

### 修复方案
根据检查结果，可能需要修复：
1. 状态转换逻辑
2. 仪表盘统计逻辑
3. 数据同步机制
