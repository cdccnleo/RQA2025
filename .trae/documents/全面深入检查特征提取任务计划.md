# 全面深入检查特征提取任务计划

## 任务目标
全面深入检查新创建的特征提取任务 `feature_task_single_002837_1771759647`，验证特征存储和技术指标生成功能是否完全修复。

## 检查维度

### 1. 任务基本信息检查
- 任务状态是否为 "completed"
- 任务配置中的 `indicators` 字段是否正确
- 任务创建时间和更新时间
- 任务特征数量是否与配置一致

### 2. 特征存储表数据检查
- `feature_store` 表中是否有该任务的特征记录
- 特征数量是否与任务记录一致
- 特征名称列表，确认是否包含技术指标
- 特征类型和参数是否正确

### 3. 技术指标生成检查
- 检查是否生成了配置的技术指标（SMA, EMA, RSI, MACD, KDJ, BOLL）
- 验证技术指标名称格式（如 `sma`, `ema_12`, `rsi_14`, `macd_line` 等）
- 检查技术指标数据是否有效（非空值）

### 4. Docker 容器日志检查
- 检查 `worker_executor.py` 执行日志
- 检查 `feature_task_executor.py` 执行日志
- 检查 `TechnicalProcessor` 处理日志
- 验证 `indicators` 参数是否正确传递

### 5. 代码执行路径检查
- 验证任务是否在 `worker_executor.py` 中执行
- 验证特征引擎是否正确调用
- 验证 `TechnicalProcessor.process` 方法是否被调用

### 6. 前端展示检查
- 检查 `get_features()` 函数返回的数据
- 验证前端特征存储仪表盘是否正确显示特征名称
- 检查特征类型和参数是否正确展示

### 7. 量化交易系统合规性检查
- 数据完整性检查
- 元数据质量检查
- 支持回测、策略优化、模型训练检查

## 检查步骤

### 步骤 1: 查询任务基本信息
```sql
SELECT task_id, status, feature_count, config, created_at, updated_at
FROM feature_engineering_tasks
WHERE task_id = 'feature_task_single_002837_1771759647';
```

### 步骤 2: 查询特征存储表数据
```sql
SELECT feature_id, feature_name, feature_type, parameters, symbol, quality_score
FROM feature_store
WHERE task_id = 'feature_task_single_002837_1771759647'
ORDER BY feature_name;
```

### 步骤 3: 分析特征数据
- 统计基础价格特征数量（open, high, low, close, volume, date）
- 统计技术指标特征数量（SMA, EMA, RSI, MACD, KDJ, BOLL）
- 检查特征名称格式

### 步骤 4: 检查 Docker 容器日志
```bash
docker logs rqa2025-app 2>&1 | grep -E "特征配置|TechnicalProcessor|indicators="
```

### 步骤 5: 验证代码执行
- 检查 `worker_executor.py` 是否执行
- 检查 `feature_task_executor.py` 是否执行
- 检查 `TechnicalProcessor.process` 是否被调用

### 步骤 6: 前端展示验证
- 调用 `get_features()` 函数
- 检查返回的特征列表
- 验证特征名称和类型

## 预期结果

### 成功标准
- ✅ 任务状态为 "completed"
- ✅ `feature_store` 表中有该任务的特征记录
- ✅ 特征数量 > 6（基础价格特征 + 技术指标）
- ✅ 包含配置的技术指标（SMA, EMA, RSI, MACD, KDJ, BOLL）
- ✅ 特征名称格式正确（如 `sma`, `ema`, `rsi`, `macd_line` 等）
- ✅ Docker 日志显示 `TechnicalProcessor` 正确处理指标
- ✅ 前端特征存储仪表盘正确显示特征

### 失败标准
- ❌ 任务状态不为 "completed"
- ❌ `feature_store` 表中没有特征记录
- ❌ 特征数量 <= 6（只有基础价格特征）
- ❌ 不包含配置的技术指标
- ❌ Docker 日志显示错误或未执行关键代码

## 修复状态

### 已完成的修复
1. ✅ `worker_executor.py` - 添加 `FeatureConfig` 配置传递
2. ✅ `feature_task_executor.py` - 添加 `FeatureConfig` 配置传递
3. ✅ `TechnicalProcessor` - 重写 `process` 方法，正确使用 `technical_indicators`
4. ✅ Docker 容器 - 重新构建，包含所有修复
5. ✅ 时钟同步 - 配置容器时钟同步

### 待验证的修复
- `TechnicalProcessor.process` 方法是否正确生成技术指标
- 特征名称格式是否正确（不是 `feature_sma` 而是 `sma`）
- 前端展示是否正确
