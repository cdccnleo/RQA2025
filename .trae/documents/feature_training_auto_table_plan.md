# 特征工程和模型训练自动创建表功能实施计划

## 计划目标
检查特征工程和模型训练的持久化实现，参考策略优化的自动创建表机制，为它们添加自动创建表功能。

## 检查范围

### 1. 特征工程持久化检查
- 检查当前表创建方式
- 检查数据库连接逻辑
- 识别需要自动创建表的位置

### 2. 模型训练持久化检查
- 检查当前表创建方式
- 检查数据库连接逻辑
- 识别需要自动创建表的位置

### 3. 自动创建表实现
- 参考策略优化的实现方式
- 为特征工程添加自动创建表
- 为模型训练添加自动创建表

## 实施步骤

### 第一阶段：代码审查（30分钟）
1. 审查特征工程持久化代码
2. 审查模型训练持久化代码
3. 对比策略优化的实现
4. 确定修改方案

### 第二阶段：特征工程修改（30分钟）
1. 添加表存在检查函数
2. 添加自动创建表逻辑
3. 在写入操作前调用创建表

### 第三阶段：模型训练修改（30分钟）
1. 添加表存在检查函数
2. 添加自动创建表逻辑
3. 在写入操作前调用创建表

### 第四阶段：测试验证（30分钟）
1. 重新构建容器
2. 验证功能正常
3. 检查日志输出

### 第五阶段：文档更新（15分钟）
1. 更新实施报告
2. 记录修改内容

## 技术方案

### 自动创建表函数模板
```python
def _ensure_table_exists():
    """确保表存在"""
    conn = _get_db_connection()
    if not conn:
        return False
    
    try:
        with conn.cursor() as cur:
            cur.execute("""
                CREATE TABLE IF NOT EXISTS table_name (
                    ...
                );
            """)
            
            # 创建索引
            cur.execute("""
                CREATE INDEX IF NOT EXISTS idx_name ON table_name(column);
            """)
            
            conn.commit()
            return True
    except Exception as e:
        logger.warning(f"创建表失败: {e}")
        return False
    finally:
        conn.close()
```

### 修改点
1. 在保存函数开始时调用 `_ensure_table_exists()`
2. 确保表结构与原SQL脚本一致
3. 添加适当的日志记录

## 预期产出
1. 更新后的feature_task_persistence.py
2. 更新后的training_job_persistence.py
3. 实施报告

## 风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| 表结构不一致 | 低 | 中 | 仔细核对原SQL脚本 |
| 索引缺失 | 低 | 低 | 完整复制索引创建语句 |
| 兼容性问题 | 低 | 低 | 保持原有逻辑不变 |
