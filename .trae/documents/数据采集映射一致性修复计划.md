## 📋 数据采集映射一致性修复计划

### 🔍 **问题分析**

**当前数据状态**：
- 总记录数：772 条
- 数据源：3 个（akshare_stock_a, historical_collection_002837, historical_collection_688702）
- 数据类型：4 种（daily, fundamental, price, volume）

**核心问题**：
1. **数据映射不一致**：
   - ✅ 符合要求：`akshare_stock_a` 数据源，`daily` 数据类型，字段完整
   - ❌ 不符合要求：`historical_collection_688702` 数据源，`price`/`volume`/`fundamental` 数据类型，缺少关键字段

2. **字段映射问题**：
   - 不符合要求的数据缺少：`pct_change`, `change`, `turnover_rate`, `amplitude` 字段值
   - 数据类型使用非标准值（应使用 `daily`/`weekly`/`monthly`）

### 🛠️ **修复计划**

#### **步骤 1：删除不符合要求的数据**
- **目标**：删除 `source_id='historical_collection_688702'` 的所有记录
- **影响范围**：363 条记录（1 条 fundamental + 181 条 price + 181 条 volume）
- **执行 SQL**：
  ```sql
  DELETE FROM akshare_stock_data WHERE source_id = 'historical_collection_688702';
  ```

#### **步骤 2：检查并修复 historical_collection_002837 数据**
- **目标**：检查该数据源的字段完整性
- **执行操作**：
  1. 检查数据结构和字段映射
  2. 修复数据类型为标准值
  3. 确保字段完整性

#### **步骤 3：验证修复结果**
- **目标**：确保数据一致性
- **验证内容**：
  1. 确认不符合要求的数据已删除
  2. 验证剩余数据的字段完整性
  3. 检查数据类型标准化情况

#### **步骤 4：建立数据采集映射标准**
- **目标**：确保历史数据采集和日常增量数据采集使用统一映射
- **标准定义**：
  - `source_id` 应为 `akshare_stock_a`
  - `data_type` 应为 `daily`/`weekly`/`monthly`
  - 所有关键字段必须完整映射

### 📊 **预期结果**

**修复前**：
- 总记录数：772 条
- 符合要求记录：46 条（akshare_stock_a）
- 不符合要求记录：726 条

**修复后**：
- 总记录数：409 条（46 + 363）
- 符合要求记录：409 条
- 数据映射一致性：100%

### ⚠️ **风险评估**

**低风险操作**：
- 删除操作仅影响不符合要求的数据
- 不影响核心业务数据（akshare_stock_a）
- 修复后数据质量提升

### 🚀 **执行时间**

**预计完成时间**：约 10 分钟
- 步骤 1：3 分钟
- 步骤 2：4 分钟
- 步骤 3：2 分钟
- 步骤 4：1 分钟

此计划将确保历史数据采集与日常增量数据采集的映射关系保持一致，提高数据质量和系统可靠性。