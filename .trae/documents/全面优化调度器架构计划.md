# 全面优化调度器架构计划

## 目标
按照分布式协调器架构设计，统一优化数据采集、特征工程、模型训练等所有调度器，实现任务类型路由到对应工作节点类型。

## 当前架构问题诊断

### 1. 调度器现状

| 调度器 | 位置 | 工作节点类型 | 问题 |
|-------|------|-------------|------|
| 特征任务调度器 | `src/features/distributed/task_scheduler.py` | 特征工作节点 (FEATURE_WORKER) | 训练任务被提交到此调度器 |
| 训练执行器池 | `src/ml/training/training_executor_manager.py` | 训练执行器 (TRAINING_EXECUTOR) | 独立运行，不从统一调度器获取任务 |
| 数据采集调度器 | 待检查 | 数据采集器 (DATA_COLLECTOR) | 待检查 |
| 推理调度器 | 待检查 | 推理工作节点 (INFERENCE_WORKER) | 待检查 |

### 2. 核心问题

1. **任务调度不统一**: 不同类型的任务提交到不同的调度机制
2. **工作节点类型未区分**: 特征工作节点执行训练任务（临时方案）
3. **缺少统一路由**: 没有根据任务类型自动路由到对应工作节点类型
4. **注册表未充分利用**: UnifiedWorkerRegistry 已存在但未与调度器集成

## 优化方案

### 方案: 统一调度器 + 类型路由（推荐）

**设计原则**:
1. 统一使用 `UnifiedWorkerRegistry` 管理所有工作节点
2. 扩展任务调度器，支持按任务类型路由
3. 特征任务 → 特征工作节点
4. 训练任务 → 训练执行器
5. 数据采集任务 → 数据采集器
6. 推理任务 → 推理工作节点

## 实施计划

### 阶段一: 检查现有调度器实现

#### 1.1 检查特征任务调度器
**文件**: `src/features/distributed/task_scheduler.py`
- 检查任务提交接口
- 检查工作节点获取任务机制
- 检查任务类型定义

#### 1.2 检查训练执行器管理
**文件**: `src/ml/training/training_executor_manager.py`
- 检查训练执行器生命周期
- 检查任务执行机制
- 检查与 UnifiedWorkerRegistry 的集成

#### 1.3 检查数据采集调度器
**文件**: 待查找
- 查找数据采集相关调度器
- 检查数据采集器实现

#### 1.4 检查推理调度器
**文件**: 待查找
- 查找推理相关调度器
- 检查推理工作节点实现

### 阶段二: 设计统一调度接口

#### 2.1 定义任务类型枚举
```python
class TaskType(Enum):
    FEATURE_EXTRACTION = "feature_extraction"  # 特征提取
    MODEL_TRAINING = "model_training"          # 模型训练
    DATA_COLLECTION = "data_collection"        # 数据采集
    MODEL_INFERENCE = "model_inference"        # 模型推理
    CUSTOM = "custom"                          # 自定义任务
```

#### 2.2 设计任务路由映射
```python
TASK_TYPE_TO_WORKER_TYPE = {
    TaskType.FEATURE_EXTRACTION: WorkerType.FEATURE_WORKER,
    TaskType.MODEL_TRAINING: WorkerType.TRAINING_EXECUTOR,
    TaskType.DATA_COLLECTION: WorkerType.DATA_COLLECTOR,
    TaskType.MODEL_INFERENCE: WorkerType.INFERENCE_WORKER,
}
```

### 阶段三: 实现统一调度器核心

#### 3.1 创建统一调度器类
**文件**: `src/distributed/coordinator/unified_scheduler.py`

```python
class UnifiedScheduler:
    """
    统一任务调度器
    
    支持多类型任务调度，根据任务类型自动路由到对应工作节点类型。
    """
    
    def __init__(self):
        self._registry = get_unified_worker_registry()
        self._task_queues: Dict[TaskType, PriorityQueue] = {}
        self._tasks: Dict[str, Task] = {}
        self._lock = threading.RLock()
        self._running = False
        
    def submit_task(self, task_type: TaskType, data: Dict, 
                    priority: TaskPriority = TaskPriority.NORMAL,
                    metadata: Dict = None) -> str:
        """提交任务到对应类型的队列"""
        
    def get_task(self, worker_id: str, worker_type: WorkerType) -> Optional[Task]:
        """工作节点获取对应类型的任务"""
        
    def _route_task_to_worker(self, task: Task) -> Optional[str]:
        """根据任务类型路由到合适的工作节点"""
```

#### 3.2 实现任务路由逻辑
- 根据任务类型选择对应的工作节点类型
- 在对应类型的工作节点中选择负载最低的节点
- 分配任务并更新状态

### 阶段四: 改造现有调度器

#### 4.1 改造特征任务调度器
**文件**: `src/features/distributed/task_scheduler.py`

**修改内容**:
1. 集成 UnifiedWorkerRegistry
2. 注册特征工作节点时指定类型为 `WorkerType.FEATURE_WORKER`
3. 只获取 `FEATURE_WORKER` 类型的任务
4. 移除训练任务执行逻辑（迁移到训练执行器）

#### 4.2 改造训练执行器管理
**文件**: `src/ml/training/training_executor_manager.py`

**修改内容**:
1. 训练执行器从统一调度器获取任务
2. 注册时指定类型为 `WorkerType.TRAINING_EXECUTOR`
3. 只执行 `MODEL_TRAINING` 类型的任务
4. 移除独立的任务队列

#### 4.3 改造/创建数据采集调度器
**文件**: 待确定

**修改内容**:
1. 创建或改造数据采集调度器
2. 注册数据采集器为 `WorkerType.DATA_COLLECTOR`
3. 执行 `DATA_COLLECTION` 类型的任务

#### 4.4 改造/创建推理调度器
**文件**: 待确定

**修改内容**:
1. 创建或改造推理调度器
2. 注册推理工作节点为 `WorkerType.INFERENCE_WORKER`
3. 执行 `MODEL_INFERENCE` 类型的任务

### 阶段五: 更新任务提交接口

#### 5.1 更新特征工程任务提交
**文件**: `src/gateway/web/feature_engineering_routes.py`

**修改内容**:
```python
# 使用统一调度器提交特征任务
scheduler.submit_task(
    task_type=TaskType.FEATURE_EXTRACTION,
    data=config,
    priority=TaskPriority.NORMAL,
    metadata={...}
)
```

#### 5.2 更新模型训练任务提交
**文件**: `src/gateway/web/model_training_routes.py`

**修改内容**:
```python
# 使用统一调度器提交训练任务
scheduler.submit_task(
    task_type=TaskType.MODEL_TRAINING,
    data=config,
    priority=TaskPriority.NORMAL,
    metadata={...}
)
```

#### 5.3 更新数据采集任务提交
**文件**: 待确定

**修改内容**:
```python
# 使用统一调度器提交数据采集任务
scheduler.submit_task(
    task_type=TaskType.DATA_COLLECTION,
    data=config,
    priority=TaskPriority.NORMAL,
    metadata={...}
)
```

### 阶段六: 更新工作节点执行器

#### 6.1 更新特征工作节点执行器
**文件**: `src/features/distributed/worker_executor.py`

**修改内容**:
1. 注册为 `WorkerType.FEATURE_WORKER`
2. 只获取 `FEATURE_EXTRACTION` 类型的任务
3. 移除训练任务执行逻辑

#### 6.2 更新训练执行器
**文件**: `src/ml/training/training_executor_manager.py`

**修改内容**:
1. 注册为 `WorkerType.TRAINING_EXECUTOR`
2. 从统一调度器获取 `MODEL_TRAINING` 类型的任务
3. 执行训练任务

### 阶段七: 前端监控页面更新

#### 7.1 更新特征工程监控页面
**文件**: `web-static/feature-engineering-monitor.html`

**修改内容**:
1. 显示特征工作节点状态
2. 显示特征任务队列状态
3. 移除训练任务相关显示

#### 7.2 更新模型训练监控页面
**文件**: `web-static/model-training-monitor.html`

**修改内容**:
1. 显示训练执行器状态
2. 显示训练任务队列状态
3. 显示训练执行器池统计

#### 7.3 创建统一调度器监控页面（可选）
**文件**: `web-static/unified-scheduler-monitor.html`

**功能**:
1. 显示所有类型工作节点状态
2. 显示所有类型任务队列状态
3. 显示任务路由统计

## 详细实施步骤

### 步骤 1: 创建统一调度器核心

```python
# src/distributed/coordinator/unified_scheduler.py

from enum import Enum, auto
from typing import Dict, List, Optional, Any
from queue import PriorityQueue
import threading
import logging
from datetime import datetime

from src.distributed.registry import (
    get_unified_worker_registry,
    WorkerType,
    WorkerStatus
)

logger = logging.getLogger(__name__)


class TaskType(Enum):
    """任务类型枚举"""
    FEATURE_EXTRACTION = "feature_extraction"
    MODEL_TRAINING = "model_training"
    DATA_COLLECTION = "data_collection"
    MODEL_INFERENCE = "model_inference"
    CUSTOM = "custom"


class TaskPriority(Enum):
    """任务优先级枚举"""
    CRITICAL = 1
    HIGH = 2
    NORMAL = 3
    LOW = 4


class Task:
    """任务数据类"""
    def __init__(self, task_id: str, task_type: TaskType, data: Dict,
                 priority: TaskPriority = TaskPriority.NORMAL,
                 metadata: Dict = None):
        self.task_id = task_id
        self.task_type = task_type
        self.data = data
        self.priority = priority
        self.metadata = metadata or {}
        self.status = "pending"
        self.created_at = datetime.now()
        self.started_at = None
        self.completed_at = None
        self.worker_id = None
        self.result = None
        self.error = None


class UnifiedScheduler:
    """
    统一任务调度器
    
    支持多类型任务调度，根据任务类型自动路由到对应工作节点类型。
    """
    
    # 任务类型到工作节点类型的映射
    TASK_TYPE_TO_WORKER_TYPE = {
        TaskType.FEATURE_EXTRACTION: WorkerType.FEATURE_WORKER,
        TaskType.MODEL_TRAINING: WorkerType.TRAINING_EXECUTOR,
        TaskType.DATA_COLLECTION: WorkerType.DATA_COLLECTOR,
        TaskType.MODEL_INFERENCE: WorkerType.INFERENCE_WORKER,
    }
    
    def __init__(self):
        """初始化统一调度器"""
        self._registry = get_unified_worker_registry()
        self._task_queues: Dict[TaskType, PriorityQueue] = {
            task_type: PriorityQueue() for task_type in TaskType
        }
        self._tasks: Dict[str, Task] = {}
        self._lock = threading.RLock()
        self._running = False
        self._scheduler_thread = None
        
        # 统计信息
        self._stats = {
            "total_tasks": 0,
            "pending_tasks": 0,
            "running_tasks": 0,
            "completed_tasks": 0,
            "failed_tasks": 0,
            "by_type": {task_type.value: {
                "total": 0,
                "pending": 0,
                "running": 0,
                "completed": 0,
                "failed": 0
            } for task_type in TaskType}
        }
        
        logger.info("UnifiedScheduler 初始化完成")
    
    def start(self) -> None:
        """启动调度器"""
        with self._lock:
            if self._running:
                return
            
            self._running = True
            self._scheduler_thread = threading.Thread(target=self._scheduler_loop)
            self._scheduler_thread.daemon = True
            self._scheduler_thread.start()
            
            logger.info("统一调度器已启动")
    
    def stop(self) -> None:
        """停止调度器"""
        with self._lock:
            if not self._running:
                return
            
            self._running = False
            
            if self._scheduler_thread:
                self._scheduler_thread.join(timeout=5)
            
            logger.info("统一调度器已停止")
    
    def submit_task(self, task_type: TaskType, data: Dict,
                    priority: TaskPriority = TaskPriority.NORMAL,
                    metadata: Dict = None) -> str:
        """
        提交任务到调度器
        
        Args:
            task_type: 任务类型
            data: 任务数据
            priority: 任务优先级
            metadata: 任务元数据
            
        Returns:
            任务ID
        """
        import uuid
        task_id = str(uuid.uuid4())
        
        task = Task(task_id, task_type, data, priority, metadata)
        
        with self._lock:
            # 添加到对应类型的队列
            self._task_queues[task_type].put((-priority.value, task_id))
            self._tasks[task_id] = task
            
            # 更新统计
            self._stats["total_tasks"] += 1
            self._stats["pending_tasks"] += 1
            self._stats["by_type"][task_type.value]["total"] += 1
            self._stats["by_type"][task_type.value]["pending"] += 1
            
            logger.info(f"提交任务: {task_id}, 类型: {task_type.value}, "
                       f"优先级: {priority.value}, 队列大小: {self._task_queues[task_type].qsize()}")
        
        return task_id
    
    def get_task(self, worker_id: str, worker_type: WorkerType) -> Optional[Task]:
        """
        工作节点获取任务
        
        Args:
            worker_id: 工作节点ID
            worker_type: 工作节点类型
            
        Returns:
            任务对象，如果没有可用任务则返回None
        """
        with self._lock:
            # 找到该工作节点类型可以处理的任务类型
            supported_task_types = [
                task_type for task_type, wt in self.TASK_TYPE_TO_WORKER_TYPE.items()
                if wt == worker_type
            ]
            
            # 按优先级尝试获取任务
            for task_type in supported_task_types:
                if not self._task_queues[task_type].empty():
                    _, task_id = self._task_queues[task_type].get()
                    task = self._tasks.get(task_id)
                    
                    if task and task.status == "pending":
                        task.status = "running"
                        task.started_at = datetime.now()
                        task.worker_id = worker_id
                        
                        # 更新统计
                        self._stats["pending_tasks"] -= 1
                        self._stats["running_tasks"] += 1
                        self._stats["by_type"][task_type.value]["pending"] -= 1
                        self._stats["by_type"][task_type.value]["running"] += 1
                        
                        logger.info(f"分配任务: {task_id} -> 工作节点: {worker_id}, "
                                   f"类型: {task_type.value}")
                        return task
            
            return None
    
    def complete_task(self, task_id: str, result: Any = None, error: str = None) -> None:
        """
        完成任务
        
        Args:
            task_id: 任务ID
            result: 任务结果
            error: 错误信息
        """
        with self._lock:
            task = self._tasks.get(task_id)
            if not task:
                logger.warning(f"完成任务失败: 任务 {task_id} 不存在")
                return
            
            task.completed_at = datetime.now()
            task.result = result
            task.error = error
            
            if error:
                task.status = "failed"
                self._stats["failed_tasks"] += 1
                self._stats["by_type"][task.task_type.value]["failed"] += 1
                logger.error(f"任务失败: {task_id}, 错误: {error}")
            else:
                task.status = "completed"
                self._stats["completed_tasks"] += 1
                self._stats["by_type"][task.task_type.value]["completed"] += 1
                logger.info(f"任务完成: {task_id}")
            
            # 更新运行中统计
            if self._stats["running_tasks"] > 0:
                self._stats["running_tasks"] -= 1
            if self._stats["by_type"][task.task_type.value]["running"] > 0:
                self._stats["by_type"][task.task_type.value]["running"] -= 1
    
    def get_scheduler_stats(self) -> Dict[str, Any]:
        """获取调度器统计信息"""
        with self._lock:
            return {
                "is_running": self._running,
                "total_tasks": self._stats["total_tasks"],
                "pending_tasks": self._stats["pending_tasks"],
                "running_tasks": self._stats["running_tasks"],
                "completed_tasks": self._stats["completed_tasks"],
                "failed_tasks": self._stats["failed_tasks"],
                "by_type": self._stats["by_type"],
                "queue_sizes": {
                    task_type.value: queue.qsize()
                    for task_type, queue in self._task_queues.items()
                },
                "active_workers": len(self._registry.get_active_workers())
            }
    
    def _scheduler_loop(self) -> None:
        """调度器主循环"""
        while self._running:
            try:
                # 自动路由任务到可用工作节点
                self._auto_route_tasks()
                
                # 休眠一段时间
                import time
                time.sleep(1)
                
            except Exception as e:
                logger.error(f"调度器循环错误: {e}")
                time.sleep(5)
    
    def _auto_route_tasks(self) -> None:
        """自动路由任务到工作节点"""
        # 获取所有可用工作节点
        available_workers = self._registry.get_available_workers()
        
        for worker in available_workers:
            # 找到该工作节点可以处理的任务类型
            supported_task_types = [
                task_type for task_type, wt in self.TASK_TYPE_TO_WORKER_TYPE.items()
                if wt == worker.worker_type
            ]
            
            # 尝试获取任务
            for task_type in supported_task_types:
                if not self._task_queues[task_type].empty():
                    # 这里可以添加更复杂的路由逻辑
                    # 例如：负载均衡、优先级排序等
                    break


# 全局调度器实例
_unified_scheduler: Optional[UnifiedScheduler] = None


def get_unified_scheduler() -> UnifiedScheduler:
    """获取全局统一调度器实例"""
    global _unified_scheduler
    if _unified_scheduler is None:
        _unified_scheduler = UnifiedScheduler()
    return _unified_scheduler
```

### 步骤 2: 改造特征工作节点

```python
# src/features/distributed/worker_executor.py

# 修改 start_worker 函数
def start_worker(worker_id: str, scheduler) -> None:
    """
    启动特征工作节点
    
    Args:
        worker_id: 工作节点ID
        scheduler: 统一调度器实例
    """
    from src.distributed.registry import get_unified_worker_registry, WorkerType, WorkerStatus
    
    registry = get_unified_worker_registry()
    
    # 注册为特征工作节点
    registry.register_worker(
        worker_id=worker_id,
        worker_type=WorkerType.FEATURE_WORKER,
        capabilities={"cpu": 4, "memory": "8GB", "features": ["technical", "fundamental"]},
        metadata={"version": "1.0"}
    )
    
    logger.info(f"特征工作节点 {worker_id} 已注册")
    
    # 主循环
    while True:
        try:
            # 更新心跳
            registry.update_heartbeat(worker_id)
            
            # 获取特征提取任务（只获取 FEATURE_EXTRACTION 类型）
            from src.distributed.coordinator.unified_scheduler import TaskType
            task = scheduler.get_task(worker_id, WorkerType.FEATURE_WORKER)
            
            if task:
                logger.info(f"特征工作节点 {worker_id} 获取任务 {task.task_id}")
                
                # 更新状态为忙碌
                registry.update_status(worker_id, WorkerStatus.BUSY)
                registry.assign_task(worker_id, task.task_id)
                
                # 执行特征提取任务
                try:
                    result = execute_feature_task(task)
                    scheduler.complete_task(task.task_id, result)
                    
                    # 更新统计
                    processing_time = time.time() - start_time
                    registry.complete_task(worker_id, processing_time)
                    
                except Exception as e:
                    scheduler.complete_task(task.task_id, error=str(e))
                    registry.fail_task(worker_id)
                
                # 恢复空闲状态
                registry.update_status(worker_id, WorkerStatus.IDLE)
            else:
                time.sleep(1)
                
        except Exception as e:
            logger.error(f"特征工作节点 {worker_id} 错误: {e}")
            time.sleep(5)
```

### 步骤 3: 改造训练执行器

```python
# src/ml/training/training_executor_manager.py

# 修改 TrainingExecutorLifecycleManager 类
class TrainingExecutorLifecycleManager:
    """训练执行器生命周期管理器"""
    
    async def start(self) -> bool:
        """启动训练执行器"""
        from src.distributed.registry import get_unified_worker_registry, WorkerType
        
        registry = get_unified_worker_registry()
        
        # 注册为训练执行器
        capabilities = {
            "cpu": self.config.cpu_cores,
            "memory": f"{self.config.memory_limit_gb}GB",
            "gpu": self.config.gpu_enabled,
            "gpu_devices": self.config.gpu_devices,
            "max_concurrent": self.config.max_concurrent_tasks
        }
        
        success = registry.register_worker(
            worker_id=self.config.executor_id,
            worker_type=WorkerType.TRAINING_EXECUTOR,
            capabilities=capabilities,
            metadata=self.config.metadata
        )
        
        if success:
            logger.info(f"训练执行器 {self.config.executor_id} 已注册")
            # 启动任务获取循环
            asyncio.create_task(self._task_fetch_loop())
            return True
        else:
            logger.error(f"训练执行器 {self.config.executor_id} 注册失败")
            return False
    
    async def _task_fetch_loop(self):
        """任务获取循环"""
        from src.distributed.coordinator.unified_scheduler import get_unified_scheduler
        from src.distributed.registry import WorkerType, WorkerStatus
        
        scheduler = get_unified_scheduler()
        registry = get_unified_worker_registry()
        
        while self._running:
            try:
                # 获取训练任务
                task = scheduler.get_task(self.config.executor_id, WorkerType.TRAINING_EXECUTOR)
                
                if task:
                    logger.info(f"训练执行器 {self.config.executor_id} 获取任务 {task.task_id}")
                    
                    # 执行训练任务
                    await self.execute_training_task(task.task_id, task.data)
                else:
                    await asyncio.sleep(1)
                    
            except Exception as e:
                logger.error(f"训练执行器任务获取错误: {e}")
                await asyncio.sleep(5)
```

## 预期效果

### 优化后架构
```
统一调度器 (UnifiedScheduler)
    ├── 特征任务队列 → 特征工作节点 (FEATURE_WORKER)
    ├── 训练任务队列 → 训练执行器 (TRAINING_EXECUTOR)
    ├── 数据采集任务队列 → 数据采集器 (DATA_COLLECTOR)
    └── 推理任务队列 → 推理工作节点 (INFERENCE_WORKER)

统一工作节点注册表 (UnifiedWorkerRegistry)
    ├── 管理所有类型工作节点
    ├── 统一心跳机制
    └── 健康检查
```

### 关键改进
1. **任务类型路由**: 根据任务类型自动路由到对应工作节点类型
2. **统一调度**: 所有任务通过统一调度器调度
3. **职责分离**: 不同类型的工作节点执行各自专业的任务
4. **可扩展性**: 易于添加新的任务类型和工作节点类型

## 风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|-----|------|------|---------|
| 改造复杂度高 | 高 | 高 | 分阶段实施，逐步替换 |
| 向后兼容性 | 中 | 高 | 保留旧接口作为回退 |
| 性能影响 | 低 | 中 | 充分测试后再上线 |
| 数据迁移 | 低 | 中 | 设计平滑迁移方案 |

## 验证计划

1. **单元测试**: 测试统一调度器各个功能
2. **集成测试**: 测试完整任务调度流程
3. **性能测试**: 测试高并发场景下的调度性能
4. **故障恢复测试**: 测试工作节点故障时的任务重分配

## 回滚计划

1. 保留现有调度器实现
2. 添加配置开关控制使用新旧调度器
3. 逐步切换，发现问题可快速回滚
